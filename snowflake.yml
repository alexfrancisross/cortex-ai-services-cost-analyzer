# Snowflake CLI Root Configuration
# Cortex AI Services Cost Analyzer Monorepo

definition_version: 2

entities:
  cortex_cost_analyzer:
    type: streamlit
    identifier:
      name: "CORTEX_AI_COST_ANALYZER" 
      schema: "CORTEX_APPS"
      database: "ANALYTICS"
    stage: "CORTEX_ANALYZER_STAGE"
    query_warehouse: "COMPUTE_WH"
    main_file: "apps/cortex-cost-analyzer/app.py"
    title: "Cortex AI Services Cost Analyzer"
    comment: "Production-ready cost analysis and reconciliation for Snowflake Cortex AI Services"
    artifacts:
      - "apps/cortex-cost-analyzer/app.py"
      - "apps/cortex-cost-analyzer/environment.yml"
      - "shared/"